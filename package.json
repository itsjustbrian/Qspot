{
  "name": "queuespot",
  "scripts": {
    "serve": "yarn run build-functions && cd tools && yarn run concurrent-serve",
    "polymer-serve": "polymer serve",
    "functions-serve": "firebase serve --only functions",
    "build-functions": "cd functions && yarn run build && cd .."
  },
  "flat": true,
  "version": "1.0.0",
  "main": "src/index.html",
  "repository": "https://github.com/fafcrumb/queuespot.git",
  "author": "fafcrumb <bri.ferch@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@polymer/lit-element": "^0.4.0",
    "@polymer/polymer": "^3.0.0-pre.13",
    "@webcomponents/webcomponentsjs": "^2.0.0-beta.2"
  }
}
